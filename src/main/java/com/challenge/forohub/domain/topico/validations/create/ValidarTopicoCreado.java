package com.challenge.forohub.domain.topico.validations.create;

import com.challenge.forohub.domain.topico.dto.CrearTopicoDTO;

public interface ValidarTopicoCreado {
    void validate(CrearTopicoDTO data);
}
